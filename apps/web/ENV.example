# Example environment variables for apps/web
# Copy values into `.env.local` (create that file locally) and set real values.

# Set to 1 to use local mock implementations for /api/trust/* endpoints during development
TRUST_API_MOCK=1

# JWT secret used to sign auth tokens (set a strong value in production)
ADMIN_JWT_SECRET=replace-with-a-strong-secret

# Optional: local SQLite DB for persistence (Prisma)
# Example (sqlite file stored in project):
DATABASE_URL="file:./dev.db"

NEXT_PUBLIC_MAPBOX_TOKEN=REPLACE_WITH_YOUR_MAPBOX_PUBLIC_TOKEN
