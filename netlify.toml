[build]
  base = "."
  # Use npm explicitly to avoid yarn workspace detection issues
  command = "npm install && cd services/identity && npm install && npm run prisma:generate && npm run build && cd - && npm run build"
  publish = "apps/web/.next"
  functions = "services/identity/netlify/functions"

[context.production.environment]
  NODE_VERSION = "20"